#!/usr/bin/env bash
set -e
if [[ ! -d "/home/dastarruer/Documents/dev/epub-editor" ]]; then
  echo "Cannot find source directory; Did you move it?"
  echo "(Looking for "/home/dastarruer/Documents/dev/epub-editor")"
  echo 'Cannot force reload with this script - use "direnv reload" manually and then try again'
  exit 1
fi

# rebuild the cache forcefully
_nix_direnv_force_reload=1 direnv exec "/home/dastarruer/Documents/dev/epub-editor" true

# Update the mtime for .envrc.
# This will cause direnv to reload again - but without re-building.
touch "/home/dastarruer/Documents/dev/epub-editor/.envrc"

# Also update the timestamp of whatever profile_rc we have.
# This makes sure that we know we are up to date.
touch -r "/home/dastarruer/Documents/dev/epub-editor/.envrc" "/home/dastarruer/Documents/dev/epub-editor/.direnv"/*.rc
